import { createSlice } from '@reduxjs/toolkit';

// Placeholder slice for future extension; no state or reducers needed
const listSlice = createSlice({
  name: 'list',
  initialState: {},
  reducers: {},
});

export default listSlice.reducer;
